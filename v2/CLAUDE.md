# Claude entry — ai-desk v2
> Auto-generated from AiRunAndRead_CLAUDE.js — DO NOT EDIT THIS FILE DIRECTLY.
> Run `node v2/AiRunAndRead_CLAUDE.js export-md > CLAUDE.md` to regenerate.
> Date: 2026-05-04

## このフォルダは ai-desk v2(All-as-Block, Versions-as-Body)
v2 は v1 の原理を単一の抽象(Block)に畳み込んだ実装
すべてが Block。Block の本体は versions の羅列(REAL)、現在の状態は派生(SHADOW)

## 読書順序(上から)
1. **AiRunAndRead_BIBLE.js** — 思想の正典(公理 A0〜A13 + Physics(LLMTyping 含む)+ BlockSchema + Taboos 15 + Vocabulary + Rituals)
2. **AiRunAndRead_ONBOARDING.js** — LLM の作業ルール集(禁忌 / Block ops / Graph ops / Constraint / Observation)
3. **AiRunAndRead_MANUAL.js** — 操作マニュアル(workflow / Virtual Heavy Function APPLY)
4. **ai-desk.js + ai-desk-core.js** — 実装 split(Zero-Dep、~1.2k 行、core が pure logic、ai-desk が CLI shell)
5. **e2e.js** — 111 e2e tests(bible-* CLI 4 件含む)+ 3d-prefab/test/ 55 tests = 計 166 全 green

## 想定読者
- primary: LLM(Claude / GPT / Gemini / 等)
- human role: supervisor。命令と判断のみ。.js を直接読まない。
- rationale: 人間時代の『読みやすさ』(抽象化・分割・カプセル化)は AI にとって情報の隠匿。本リポは逆方向 — 展開・明示・統合された複雑性に最適化されている。AI 用ドキュメントは .md でなく AiRunAndRead_*.js が canonical。

## v1(legacy)との関係
### layout
- **repo_root**: /PJs/ai-desk/
- **v1_main**: ../ai-desk-old-v1.js
- **v1_docs**: ../AI_NATIVE_MASTER_BIBLE.md など、リポルート配下
- **v2_canonical**: ./
### rules
- v1 のファイルは触らない、固定された過去として保持
- v2 で作業するときは v2 の正典(AiRunAndRead_BIBLE.js)に従う

## 作業の前提
- すべて Block 抽象で動く(Block { id, type, versions, meta })
- マーカー(// [ai_s_emblem:...])は新規に書かない、tags は // @tags: で書く
- TypeScript・build・依存ライブラリは禁忌
- Block.versions は append-only(commit() 経由のみ)
- Block.content / refs / tags は SHADOW(getter から都度取得、変数化しない)

## 隣接 module
- **3dplus/**: 3D を扱うコードを書く時の CPU Twin(81 tests, CommonJS)
- **eyes/**: AI-Eyes 観測ハーネス(in-memory virtual canvas、draw_op log を Block 化)
- **demos/**: v2 思想を体現する demo 群(gravity-battle / block-spreadsheet / node-graph / kanban)